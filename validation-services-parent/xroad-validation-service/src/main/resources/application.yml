server:
  port: 8081

management:
  health:
    diskspace:
      enabled: false
  endpoints:
    web:
      exposure:
        include: health
      base-path: /monitoring
  endpoint:
    health:
      show-details: "ALWAYS"
