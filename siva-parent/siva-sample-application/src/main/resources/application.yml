server:
  port: 9000
spring:
  http:
    multipart:
      max-file-size: "10Mb"
  cache:
    caffeine:
      spec: maximumSize=100,expireAfterAccess=20s
management:
  context-path: /monitoring
security:
  basic:
    path: /monitoring/**